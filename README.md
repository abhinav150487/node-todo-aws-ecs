## Project Overview

This project demonstrates how to containerize a Node.js Todo application using Docker, store the image in Amazon Elastic Container Registry (ECR), and deploy it on Amazon Elastic Container Service (ECS) using AWS Fargate.

The project showcases containerization, cloud deployment, IAM configuration, and centralized logging using Amazon CloudWatch.
